pipeline {
    agent { label "test" }
    environment {
    }
    stages {
        stage('ymlclone') {
            steps {
                    sh """
                        echo "hello pipeline"
                    """
            }
        }
    }
}

